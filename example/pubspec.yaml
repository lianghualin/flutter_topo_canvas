name: flutter_topo_canvas_example
description: Example app for flutter_topo_canvas.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.4.4
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_topo_canvas:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
